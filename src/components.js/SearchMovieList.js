import React from "react";

const SearchMovieCards = (props) => {

  console.log(props.imageUrl);

  return <div>
    {/* {props.map((movie)=>{

    })} */}
  </div>;
};

export default SearchMovieCards;
